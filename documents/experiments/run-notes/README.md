# Run Notes

This folder stores concise tracked summaries of important experiment runs.

Raw outputs such as checkpoints, metrics logs, and preprocessing ledgers live under ignored runtime directories like `runs/` and `data/`.

Each note here should preserve:

- the run ID,
- the config used,
- the hardware,
- the high-level outcome,
- and the local artifact paths needed to inspect the raw outputs later.

