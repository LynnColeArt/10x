# Fixtures

This folder contains small local assets used to verify the training harness.

## Included Fixtures

- `sample-baseline/`: a tiny mixed-domain corpus for smoke tests.
- `eval/baseline_probes.json`: simple deterministic probes used by the harness.

These fixtures exist to validate code paths, checkpointing, metric output, and run-note generation.

They are not the intended final research dataset.

