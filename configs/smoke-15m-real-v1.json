{
  "name": "smoke-15m-real-v1",
  "description": "First real-source smoke baseline on the RTX 4070 using the local byte tokenizer and a public-source local manifest.",
  "tags": ["baseline", "smoke-15m", "real-corpus", "byte-tokenizer"],
  "paths": {
    "manifest_path": "data/real-smoke-v1/manifest.json",
    "eval_probes_path": "fixtures/eval/baseline_probes.json",
    "output_dir": "runs"
  },
  "model": {
    "hidden_size": 336,
    "num_layers": 9,
    "num_heads": 6,
    "mlp_ratio": 4.0,
    "dropout": 0.0,
    "max_position_embeddings": 512,
    "norm_type": "rmsnorm",
    "activation": "swiglu",
    "rope_theta": 10000.0
  },
  "training": {
    "sequence_length": 512,
    "micro_batch_size": 4,
    "gradient_accumulation_steps": 1,
    "learning_rate": 0.0004,
    "weight_decay": 0.1,
    "warmup_steps": 10,
    "max_steps": 60,
    "log_every_steps": 10,
    "eval_every_steps": 20,
    "save_every_steps": 20,
    "max_eval_batches": 8,
    "max_grad_norm": 1.0,
    "beta1": 0.9,
    "beta2": 0.95,
    "device": "auto",
    "precision": "auto",
    "compile_model": false,
    "seed": 1234,
    "dataloader_workers": 0
  }
}

